name: Playwright Tests

permissions:
  contents: write

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  update-snapshots:
    runs-on: ubuntu-latest

    env:
      TESTING_PAGE: ${{ secrets.TESTING_PAGE }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      # ⭐ REQUIRED: authenticate git with the GitHub token
      - name: Configure Git for pushing
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}

      - name: Update snapshots
        run: npx playwright test --update-snapshots

      # ⭐ REQUIRED: pull + rebase before pushing
      - name: Commit updated snapshots
        run: |
          git fetch origin main
          git rebase origin/main || git rebase --skip
          git add .
          git commit -m "Update Linux snapshots" || echo "No changes to commit"
          git push

  test:
    needs: update-snapshots
    timeout-minutes: 60
    runs-on: ubuntu-latest

    env:
      TESTING_PAGE: ${{ secrets.TESTING_PAGE }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Cache node modules
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-npm-${{ hashFiles('package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-npm-

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
